# Rules

Versioned declarative YAML: `constraints.yaml` (verifier V4 rules engine, M4),
`theme_map.yaml` (AI-theme exposure curated mapping, M3), `scenarios.yaml` (stress scenario
library, M3), `policy.yaml` (guardrail prohibited-language patterns, M5). See
`docs/architecture.md` §4.5, §4.6, §7.5.
